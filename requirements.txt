# Maintained for pip compatibility (runtime dependencies only).
# Source of truth is pyproject.toml — edit dependencies there.
# For dev/test tools (pytest), run: pip install pytest
langfuse>=3.0,<4.0
openai>=1.0
python-dotenv>=1.0
datasets>=2.0
anthropic>=0.39
fastapi>=0.115
uvicorn[standard]>=0.30
jinja2>=3.1
cachetools>=5.0
